// scent-page.tsx holds the actual component (kept as a separate file from
// day one); this file just satisfies the Next.js App Router's page.tsx
// naming convention so /scent/[sku] is actually a reachable route.
export { default } from "./scent-page";
